<%= form_with(model: prof2, local: true) do |form| %>
 

  
  <% if prof2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prof2.errors.count, "error") %> prohibited this prof2 from being saved:</h2>

      <ul>
        <% prof2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :atype %>
    <%= form.text_field :atype %>
  </div>

  


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
